// The art archive's UI: the /werke facet search. See README.md.
//
// Nuxt auto-registers every directory under `layers/`, so this layer is active
// whether or not the root config names it in `extends` — it names it anyway, so
// the set of layers can be read in one place.
export default defineNuxtConfig({})
